<% flash.each do |type, msg| %>

  <div class="message alert <%= bootstrap_class_for_flash(type) %> alert-dismissable fade show fixed-top" role="alert">
    <%= msg %>
  <button type="button" class="close" data-dismiss="alert" aria-label="Close">
    <span aria-hidden="true">×</span>
  </button>
  </div>

<% end %>